{
  "compilerOptions": {
    "target": "ES6",
    "module": "commonjs",
    "outDir": "src/dist", // Directorio de salida para los archivos compilados
    "rootDir": "src", // La raíz de los archivos fuente
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "typeRoots": ["./node_modules/@types", "./src/types"], // Actualiza la ruta a la carpeta `types`
    "baseUrl": "./", // Esto define la base de las rutas
    "paths": {
      "@/*": ["src/*"] // Alias para la carpeta src
    }
  },
  "include": ["src/**/*.ts"], // Incluye todos los archivos .ts en la carpeta src
  "exclude": ["node_modules"]
}
