export const turnMapping: { [key: string]: number } = {
  Mañana: 1,
  Tarde: 2,
};

export const paymentMapping: { [key: string]: number } = {
  Efectivo: 1,
  Transferencia: 2,
  Débito: 3,
  Crédito: 4,
};

export default { turnMapping, paymentMapping };
